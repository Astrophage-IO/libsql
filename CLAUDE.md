# Project Rules

## Git Policy
- **NEVER run `git push` in any form.** This includes `git push`, `git push --force`, `git push -u`, `git push origin`, etc.
- Only `git commit` is allowed. The owner controls when and how code is pushed.
- Do not use `gh` CLI (GitHub CLI) for any operations.
- Do not create pull requests.
